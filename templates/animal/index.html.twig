{% extends 'base.html.twig' %}

{% block title %}Mes animaux - PetVeto{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-4">
        <h2 class="fw-bold">Bienvenue {{ app.user.firstname }} 👋</h2>
        <h1 class="display-6">Mes animaux</h1>
    </div>

    {% if animals is empty %}
        <div class="alert alert-info text-center">
            <p>Aucun animal enregistré pour le moment 🐾</p>
            <p>Cliquez sur le bouton ci-dessous pour en ajouter un.</p>
        </div>
    {% else %}
        <div class="row justify-content-center">
            {% for animal in animals %}
                <div class="col-sm-10 col-md-6 col-lg-4 mb-4">
                    <div class="card shadow-sm h-100">
                        <img src="https://place-puppy.com/400x250" class="card-img-top" alt="Photo de {{ animal.name }}">
                        <div class="card-body text-center">
                            <h5 class="card-title fw-semibold">Nom de l'animal : {{ animal.name }}</h5>
                            <p class="card-text text-muted">Espèce de l'animal : {{ animal.species }}</p>
                            <div class="d-grid gap-2">
                                <a href="{{ path('animal_care_list', { id: animal.id }) }}"  class="btn btn-outline-secondary">
                                    🩺 Voir les soins
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}

    <div class="row mt-5 justify-content-center gap-3">
        <div class="col-10 col-md-4 col-lg-3 d-grid">
            <a href="{{ path('animal_new') }}" class="btn btn-success btn-lg">
                ➕ Ajouter un animal
            </a>
        </div>
        <div class="col-10 col-md-4 col-lg-3 d-grid">
            <a href="{{ path('app_home') }}" class="btn btn-outline-primary btn-lg">
                🏠 Retour à l'accueil
            </a>
        </div>
    </div>
</div>
{% endblock %}
