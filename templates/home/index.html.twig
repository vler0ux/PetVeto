{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur Pet Veto{% endblock %}

{% block body %}
   <div class="row g-4 justify-content-center">
        <div class="col-12 col-md-5 col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-body d-flex flex-column justify-content-center">
                    <h5 class="card-title mb-3">👤 Espace Utilisateur</h5>
                    <a href="{{ path('app_login_user') }}" class="btn btn-primary mb-2">
                        Se connecter
                    </a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-primary">
                        Créer un compte
                    </a>
                </div>
            </div>
        </div>

        <div class="col-12 col-md-5 col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-body d-flex flex-column justify-content-center">
                    <h5 class="card-title mb-3">🩺 Espace Vétérinaire</h5>
                    <a href="{{ path('app_login_veto') }}" class="btn btn-success mb-2">
                        Se connecter
                    </a>
                    <a href="{{ path('veto_new') }}" class="btn btn-outline-success">
                        Créer un compte
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
